[pytest]
pythonpath = ..
testpaths = .
addopts = -q -ra
markers =
    toolchain: real local compiler or formal-tool subprocesses
    protocol: real backend process and WebSocket transport
    live_llm: opt-in real LLM/network execution
